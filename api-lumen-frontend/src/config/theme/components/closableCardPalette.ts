import { ClosableCardPalette } from '@fattureincloud/fic-design-system'

import { ficPalette } from '../palette'

const closableCardPalette: ClosableCardPalette = {
  background: ficPalette.white[80],
  border: ficPalette.grey[200],
}

export default closableCardPalette
